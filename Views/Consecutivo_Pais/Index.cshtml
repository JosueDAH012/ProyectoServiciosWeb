@model IEnumerable<V_VuelosServiciosWeb.Models.CONSECUTIVO_PAIS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="bg-image" style=" background-image: url('https://images.pexels.com/photos/46148/aircraft-jet-landing-cloud-46148.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=750&w=1260');height: 100vh; ">


    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CONSECUTIVO_PAIS1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PREFIJO_PAIS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RANGO_INICIAL_PAIS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RANGO_FINAL_PAIS)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CONSECUTIVO_PAIS1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PREFIJO_PAIS)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RANGO_INICIAL_PAIS)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RANGO_FINAL_PAIS)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_CONSECUTIVO_PAIS }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_CONSECUTIVO_PAIS }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_CONSECUTIVO_PAIS })
            </td>
        </tr>
    }

</table>
</div>
