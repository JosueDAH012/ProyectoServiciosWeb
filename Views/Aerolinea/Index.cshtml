@model IEnumerable<V_VuelosServiciosWeb.Models.AEROLINEA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/LayoutCliente.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_AEROLINEA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IMAGEN_AEROLINEA.ID_IMAGEN_AEROLINEA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_AEROLINEA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IMAGEN_AEROLINEA.ID_IMAGEN_AEROLINEA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_AEROLINEA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_AEROLINEA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_AEROLINEA })
        </td>
    </tr>
}

</table>
