@model IEnumerable<V_VuelosServiciosWeb.Models.IMAGEN_PAIS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/LayoutCliente.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IMAGEN_PAIS1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IMAGEN_PAIS1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_IMAGEN_PAIS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_IMAGEN_PAIS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_IMAGEN_PAIS })
        </td>
    </tr>
}

</table>
