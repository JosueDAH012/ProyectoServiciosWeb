@model IEnumerable<V_VuelosServiciosWeb.Models.CONSECUTIVO_VUELO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/LayoutCliente.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CONSECUTIVO_VUELO1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PREFIJO_VUELO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RANGO_INICIAL_VUELO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RANGO_FINAL_VUELO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CONSECUTIVO_VUELO1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PREFIJO_VUELO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RANGO_INICIAL_VUELO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RANGO_FINAL_VUELO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_CONSECUTIVO_VUELO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_CONSECUTIVO_VUELO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_CONSECUTIVO_VUELO })
        </td>
    </tr>
}

</table>
